    const specMaleBicepCurl = {
        $schema: 'https://vega.github.io/schema/vega-lite/v5.json',
        // Paste the JSON string from chart.to_json() here
    };

    vegaEmbed('#boxplot-bicep-curl', specMaleBicepCurl, { "actions": false }).catch(console.error);

